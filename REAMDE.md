Python scripts for Switch Mode Power Supply (SMPS) design.

This is a big-time work in progress.

Run `pip install -r requirements.txt` to install dependencies.

Try out `compensation.py`, for example.